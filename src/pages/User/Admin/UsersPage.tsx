import UserManagement from "@/components/User/controlpanel/UserManagement";

/**
 * Clean entry point: Only calls the self-contained component.
 */
export default function UsersPage() {
  return <UserManagement />;
}